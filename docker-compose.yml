services:
  mongodb:
    image: mongo:7
    container_name: conventioner_mongodb
    restart: unless-stopped
    command: ["mongod", "--quiet", "--logpath", "/var/log/mongodb/mongod.log"]
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: secret
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
      - ./back-end/mongo-init.js:/docker-entrypoint-initdb.d/mongo-init.js:ro
    networks:
      - conventioner_network
    healthcheck:
      test: echo 'db.runCommand("ping").ok' | mongosh localhost:27017/test --quiet
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./back-end
      dockerfile: Dockerfile
    container_name: conventioner_backend
    restart: unless-stopped
    environment:
      - FLASK_APP=app.py
      - FLASK_ENV=development
      - USE_HTTPS=true
      - MONGODB_HOST=mongodb
      - MONGODB_PORT=27017
      - MONGODB_USER=admin
      - MONGODB_PASSWORD=secret
      - MONGODB_DB=conventioner
      - RESEND_API_KEY=${RESEND_API_KEY:-}
      - FRONTEND_URL=${FRONTEND_URL:-http://localhost:5173}
      - FROM_EMAIL=${FROM_EMAIL:-onboarding@resend.dev}
      - RECAPTCHA_SECRET_KEY=${RECAPTCHA_SECRET_KEY:-}
      - DISABLE_CAPTCHA=${DISABLE_CAPTCHA:-}
      - GEMINI_API_KEY=${GEMINI_API_KEY:-}
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
    ports:
      - "5000:5000"
    volumes:
      - ./back-end:/app
      - backend_sessions:/app/flask_session
      - backend_csv:/app/csv_exports
    depends_on:
      mongodb:
        condition: service_healthy
    networks:
      - conventioner_network
    command: flask run --host=0.0.0.0 --port=5000 --cert=adhoc

  frontend:
    build:
      context: ./front-end
      dockerfile: Dockerfile
    container_name: conventioner_frontend
    restart: unless-stopped
    environment:
      - VITE_FLASK_HOST=/api
      - VITE_BACKEND_URL=https://backend:5000
      - VITE_RECAPTCHA_SITE_KEY=${RECAPTCHA_SITE_KEY:-}
    ports:
      - "5173:5173"
    volumes:
      - ./front-end:/app
      - /app/node_modules
    depends_on:
      - backend
    networks:
      - conventioner_network

  mongo-express:
    image: mongo-express:latest
    container_name: conventioner_mongo_express
    restart: unless-stopped
    ports:
      - "8081:8081"
    environment:
      - ME_CONFIG_MONGODB_ADMINUSERNAME=admin
      - ME_CONFIG_MONGODB_ADMINPASSWORD=secret
      - ME_CONFIG_MONGODB_SERVER=mongodb
      - ME_CONFIG_MONGODB_PORT=27017
      - ME_CONFIG_MONGODB_URL=mongodb://mongodb:27017
      - ME_CONFIG_BASICAUTH_USERNAME=admin
      - ME_CONFIG_BASICAUTH_PASSWORD=admin
    depends_on:
      mongodb:
        condition: service_healthy
    networks:
      - conventioner_network

networks:
  conventioner_network:
    driver: bridge

volumes:
  mongodb_data:
    driver: local
  backend_sessions:
    driver: local
  backend_csv:
    driver: local
